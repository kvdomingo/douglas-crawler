apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "douglas-crawler.fullname" . }}-test-connection"
  labels:
    {{- include "douglas-crawler.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "douglas-crawler.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
