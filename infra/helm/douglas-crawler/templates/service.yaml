apiVersion: v1
kind: Service
metadata:
  name: {{ include "douglas-crawler.fullname" . }}
  labels:
    {{- include "douglas-crawler.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "douglas-crawler.selectorLabels" . | nindent 4 }}
